name: vizxr
description: "Vizxr — High-fidelity Kubernetes IDE for Android"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  kubernetes: ^1.26.0+5
  dartssh2: ^2.1.0
  xterm: ^4.0.0
  flutter_svg: ^2.0.10+1
  google_fonts: ^8.0.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
